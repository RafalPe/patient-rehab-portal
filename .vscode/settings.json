{
  // Tailwind CSS v4 uses custom at-rules like @theme and @import "tailwindcss"
  // which are not recognized by VS Code's built-in CSS linter.
  "css.lint.unknownAtRules": "ignore",

  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.formatOnSave": true
}
